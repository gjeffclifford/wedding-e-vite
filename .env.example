# Optional fallback RSVP endpoint for local development.
# Each wedding can still set rsvp.apiUrl in its configuration.
# This is a Google Apps Script Web App URL — never put Google credentials here.
VITE_RSVP_API_URL=
